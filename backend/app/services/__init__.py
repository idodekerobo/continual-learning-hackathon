"""
Service-layer modules (integrations + orchestration).

In this hackathon build we start with stubs and progressively replace them with
real implementations (Composio, You.com, OpenAI, persistence, etc.).
"""

